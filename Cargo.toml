[package]
name = "struct-input"
version = "0.0.1"
edition = "2024"
authors = ["Yunseong Jeong <me@yunseong.dev>"]
description = "A server monitoring and remote control tool via messenger."
license = "MIT"

[dependencies]
struct-input-derive = { version = "0.0.1", path = "./struct-input-derive" }

regex = "1.12.3"
tokio = { version = "1.49.0", features = ["full"] }
